apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-service-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sample-service-mongo
  template:
    metadata:
      labels:
        app: sample-service-mongo
    spec:
      containers:
        - name: sample-service-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: sample-service-mongo-srv
spec:
  selector:
    app: sample-service-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
